import XCTest
@testable import VishalAppFramework

final class VishalAppFrameworkTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(VishalAppFramework().text, "Hello, World!")
    }
}
